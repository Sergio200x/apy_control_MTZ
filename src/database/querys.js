export const querys ={
   
    equipos_prp:"use MOSTAZA_ERP   select  LTRIM(RTRIM(v.local)) as 'Codigolocal',  IIF(v.PARAMETRO='VERSION','Profit',v.parametro) as 'Aplicacion',    rtrim(ltrim(v.valor)) as 'Nversion',  MAX(v.FECHATRANS) as 'FechaAct',   RTRIM(LTRIM(v.EQUIPO))AS EQUIPO  ,   RTRIM(LTRIM(v.caja))AS Caja  from hparamloc AS V   where v.PARAMETRO like 'version'   and v.FECHATRANS > getdate()-1  GROUP BY v.empresa, v.local, v.parametro, v.valor,v.EQUIPO,v.caja order by v.local ,V.EQUIPO",
    locales_prp:"select distinct rtrim(ltrim(v.local)) as 'Codigolocal' , rtrim(ltrim(l.local_nombre)) as 'Nombrelocal' from hparamloc AS V   INNER JOIN [MOSTAZA_ERP].[dbo].[locales] AS L ON L.local_codigo = V.LOCAL     where v.PARAMETRO like 'version'   and v.FECHATRANS > getdate()-1  GROUP BY v.LOCAL,l.local_nombre ",
    versiones_prp:"use MOSTAZA_ERP  select LTRIM(RTRIM(v.local)) as 'Codigo_Local',rtrim(ltrim(substring(l.local_nombre,0,20))) as 'Nombre_local',iif( v.PARAMETRO='VERSION','Profit', rtrim(ltrim(v.parametro))) as 'Parametro', rtrim(ltrim(replace(replace(v.valor,'.:: v',''),'::.',''))) as 'Nro_version',MAX( substring(convert(varchar,v.FECHATRANS,103),0,15)) as 'FechaTrans',RTRIM(LTRIM(substring(v.EQUIPO,0,15)))AS EQUIPO  ,RTRIM(LTRIM(v.caja))AS Caja from  hparamloc AS V INNER JOIN [MOSTAZA_ERP].[dbo].[locales] AS L ON L.local_codigo = V.LOCAL where v.PARAMETRO  in ('Informes|Version','PanelRappi|VERSION','PanelPedidosYa|VERSION','PanelMTZ|VERSION','ActualizaDatos|VERSION','Centralizador','CentralizadorComanda|Version','VERSION','PanelMELI|VERSION','DescargaLocal|VERSION','ZonaEntrega|Version','ZonaLlamador|Version','PantallaComanda|Version','CFOCXVERSI','VERSIONT','Informes') and v.FECHATRANS > getdate()-1 GROUP BY v.empresa, v.local,l.local_nombre, v.parametro, v.valor,v.EQUIPO,v.caja order by L.local_nombre, V.EQUIPO"	
}